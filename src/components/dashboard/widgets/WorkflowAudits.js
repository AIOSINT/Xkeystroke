import React from 'react';

const WorkflowAudits = ({ count }) => {
    return (
        <div className="widget">
            <h3>Workflow Audits</h3>
            <p>{count}</p>
        </div>
    );
};

export default WorkflowAudits;
