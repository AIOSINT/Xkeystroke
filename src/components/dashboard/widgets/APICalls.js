import React from 'react';

const APICalls = ({ count }) => {
    return (
        <div className="widget">
            <h3>API Calls</h3>
            <p>{count}</p>
        </div>
    );
};

export default APICalls;
